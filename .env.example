# This project does not require environment variables for Vercel deployment.
#
# If you add browser-side environment variables later, Vite requires names
# to start with VITE_, for example:
# VITE_API_URL=https://example.com
